diff --git a/auto/configure b/auto/configure
index 7e6e33a7..bb368cfb 100755
--- a/auto/configure
+++ b/auto/configure
@@ -100,6 +100,7 @@ have=NGX_HTTP_SCGI_TEMP_PATH value="\"$NGX_HTTP_SCGI_TEMP_PATH\""
 . auto/define
 
 . auto/make
+. auto/make_fuzzers
 . auto/lib/make
 . auto/install
 
diff --git a/auto/make b/auto/make
index 34c40cdd..f673eeb7 100644
--- a/auto/make
+++ b/auto/make
@@ -224,7 +224,7 @@ ngx_main_link=${MAIN_LINK:+`echo $MAIN_LINK \
 
 cat << END                                                    >> $NGX_MAKEFILE
 
-build:	binary modules manpage
+build:	fuzzers modules manpage
 
 binary:	$NGX_OBJS${ngx_dirsep}nginx$ngx_binext
 
diff --git a/src/core/nginx.c b/src/core/nginx.c
index f73e5598..dddf3fc6 100644
--- a/src/core/nginx.c
+++ b/src/core/nginx.c
@@ -191,8 +191,29 @@ static char        *ngx_signal;
 static char **ngx_os_environ;
 
 
+
+int __wrap_bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen){
+  return 0;
+}
+
+
+int __wrap_shutdown(int socket, int how){
+  return 0;
+}
+
+
+ssize_t __wrap_listen(int fd, void* buf, size_t bytes) {
+  return 0;
+}
+
+
+int __wrap_setsockopt(int fd, int level, int optname, const void *optval, socklen_t optlen) {
+  return 0;
+}
+
+
 int ngx_cdecl
-main(int argc, char *const *argv)
+fuzzer_removed_main(int argc, char *const *argv)
 {
     ngx_buf_t        *b;
     ngx_log_t        *log;
